mtcars
mtcars
head(mtcars)
tail(mtcars)
dim(mtcars)
str(mtcars)
summary(mtcars)
library(dplyr)
install.packages(c("dplyr", "ggplot2", "tidyr", "tidyverse"))
library(dplyr)
library(dplyr)
mtcars %>% filter(mpg > 25) %>% select(mpg, gear, cyl) %>% head(10)
mtcars %>% filter(mpg > 19) %>% select(mpg, gear, cyl) %>% head(10)
head(mtcars)
mtcars %>% filter(cyl > 4) %>% select(mpg, gear, cyl)
mtcars %>% filter(cyl > 4,mpg > 19) %>% select(mpg, gear, cyl)
mtcars %>% summarise(mean (mpg),mean(wt) )
mean(mtcars)
colMeans(mtcars)
rowMeans(mtcars)
mean(mtcars$mpg)
sd(mtcars$mpg)
cor(mtcars)
cor(mtcars$mpg, mtcars$wt)
cor(mtcars$mpg, mtcars$hp)
corrplot(cor(mtcars),
method = "color",
type = "upper")
corrplot(cor(mtcars), method = "color", type = "upper")
corrplot(
cor(mtcars),
method = "color",
type = "upper"
)
